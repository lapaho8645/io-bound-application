spring:
  datasource:
    url: jdbc:postgresql://10.140.0.3:5432/postgresql
    # url: jdbc:postgresql://localhost:5432/postgresql
    username: postgresql
    password: postgrespassword
  jpa:
    show-sql: false
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
  rabbitmq:
    host: 10.146.0.2  #rabbitmq 인스턴스의 내부아이피
    username: guest
    password: guest
    port: 5672